const {
    get,
    set,
    update,
    del,
    has,
    generateObjectFromPath,
    merge,
    destructure,
    contains,
    equals,
} = require('./objectivize');
const { keys, values, entries } = require('./prototype');

module.exports = {
    get,
    set,
    update,
    del,
    has,
    generateObjectFromPath,
    merge,
    destructure,
    contains,
    equals,
    keys,
    values,
    entries
};